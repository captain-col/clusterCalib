#ifndef EClusterRecon_hxx_seen
#define EClusterRecon_hxx_seen

#include <ECore.hxx>

namespace CP {
    /// Generate a base exception for the Analysis library
    EXCEPTION(EClusterRecon,ECore);
};
#endif
