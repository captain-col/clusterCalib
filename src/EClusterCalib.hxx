#ifndef EClusterCalib_hxx_seen
#define EClusterCalib_hxx_seen

#include <ECore.hxx>

namespace CP {
    /// Generate a base exception for the Analysis library
    EXCEPTION(EClusterCalib,ECore);
};
#endif
